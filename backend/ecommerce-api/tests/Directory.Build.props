<Project>
  <!-- Import solution-wide settings from backend/ecommerce-api/Directory.Build.props -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <!-- Test projects under tests/ are not packable NuGet artifacts. -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <PropertyGroup Condition="$(MSBuildProjectName.EndsWith('.Tests')) Or $(MSBuildProjectName.Contains('.UnitTests')) Or $(MSBuildProjectName.Contains('.IntegrationTests'))">
    <IsTestProject>true</IsTestProject>
  </PropertyGroup>
</Project>
